#include <iostream>
using namespace std;
int main() {
    int n, m ;
    cout<<"Enter a number for addition: ";
    cin>>n;
    cout<<"Enter another number for addition: ";
    cin>>m;
     int sum=n+m;
    cout<<"Sum of "<<n<<" and "<<m<<" is : "<<sum;

}